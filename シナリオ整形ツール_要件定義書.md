# シナリオ整形・セッション支援ツール 要件定義書

## 0. 改訂にあたって

### 0.1 第1回改訂(2026-07-23、大幅方針転換)

当初はPDF・TXTのシナリオをNotionのトグル構造に変換するツール(Notionへのコピペ効率化が主目的)として要件を積み上げていたが、2026-07-23にMikotoから方針転換の依頼があった。転換後の骨子は以下の通り。

- ツールの主な出力は「Notion貼り付け用Markdown」ではなく、トグル見出し・ジャンプ目次・NPCカード・チャットパレットコピー・内部リンクなどを備えた**セッション本番で直接使うインタラクティブなHTML**に変わる。
- 生成したHTMLはiPhone・iPadでそのまま開いて、セッション中の描写・情報コピペに使う。
- Notion向けMarkdown出力は残るが、位置づけは「セッション後の記録用エクスポート」に変わり、書式も一から再検討する。
- 見出し検出(記号指定方式)・階層構造化・プレビュー手動調整など、これまで検証してきた基盤ロジックはそのまま活用する。

### 0.2 第2回改訂(2026-07-23、見出し検出方式の再転換・編集UIの追加)

その後さらにMikotoから追加要望があり、以下を反映した。

- **見出し検出を完全手動方式に変更。** 記号指定による半自動検出(0.1時点の方式)は、記号パターンが作者ごとに大きく異なりすぎるため撤回し、ユーザーが本文を直接選択して見出しを設定する方式にした(5.4参照)。
- **編集モード/プレビューモードの追加。** 見出し設定・コピー&ペーストボタンの設置・文字装飾を行うための「編集用ツールボックス」(Wordのリボンのようなイメージ)を画面上部に表示する編集モードと、ツールボックスを隠して完成形だけを見せるプレビューモードを分ける(5.2・5.3参照)。
- **文字装飾機能(下線・イタリック・ボールド)を追加。**

以下、上記2回の改訂を反映した最新の要件定義。検証結果などの記録は、後で経緯を追えるようにあえて残してある。

## 1. 目的・背景

TRPGセッションの準備段階およびセッション中における、シナリオ整形・コピペ作業の負担を軽減するツールを作成する。準備段階では、PDF・TXTで受け取ったシナリオの内容把握そのものに時間がかかること、セッションで使いやすい体裁へ整形する作業が手間であることが課題として挙がっている。セッション中には、NPCの情報や地の文・判定情報・探索情報をコピペする作業が発生しており、この2つは準備段階の整形作業とセットで負担になっている。

本ツールは、PDF・TXTのシナリオを読み込み、ユーザーが手動で見出し・装飾・コピー用ボタンを設定すると、トグル見出し・ジャンプ目次・NPCカードなどを備えたセッション本番用のHTMLに変換できるようにし、準備〜セッション中〜セッション後(Notion記録)までを横断して負担を軽減することを目的とする(2026-07-23改訂)。

## 2. 想定利用者

Mikoto本人(GMとしてシナリオ準備・セッション進行を行う利用者)。CClogと同様、「非エンジニア」を想定し、専門知識不要・平易な日本語UIを前提として設計する(Mikoto確認済み、2026-07-23)。

## 3. ツール構成・利用環境・制約

- **ツール構成:** 変換機能(PDF/TXT→HTML生成)と、生成済みHTMLの読み込み・プレビュー機能を、1つの同じツールに持たせる(Mikoto確認済み、2026-07-23)。ツールを開いてシナリオのPDF/TXTを読み込むと編集モードの画面が表示され、逆に一度生成したHTMLファイルを読み込ませれば、そのままプレビュー・再編集できる想定。
- PC・モバイル(iPhone/iPad)の両方で使用できることを前提とする(Mikoto確認済み、2026-07-23)。
- ホスティング形態・技術構成は、CClogと同じ構成(GitHub Pages + クライアントサイド完結)を踏襲する(Mikoto確認済み、2026-07-23)。サーバー・データベース・ログイン機能を持たず、ブラウザ内で完結させる方針もCClogに準ずる。
- **モバイル動作のリスク(2026-07-23、参考ツール調査で判明):** 類似の先行ツールを2点調査した(12章参照)ところ、いずれもスマートフォン・タブレットでの動作を保証していないことが分かった(1点はWindows専用アプリ、もう1点はPC向けブラウザ動作確認のみでスマホ・タブレットは動作保証外)。今回のツールは「NPCカード管理」「トグル見出し」「ジャンプ目次」「編集用ツールボックス」など状態を多く持つリッチなHTMLになる見込みで、同種の先行ツールが軒並みモバイル動作を保証していない点は無視できないリスクと考えられる(推測です)。

- **iOS Safari固有の技術的リスク(2026-07-23、Web調査による。実機での動作確認ではない点に注意):**
  - **ファイルのダウンロード(`<a download>`属性):** 複数の情報源で、iOS SafariはPC版Safariと異なり`<a download>`属性に対応していないと報告されている。HTML・Markdown・TXTの書き出し(5.14)は、CClogも含めこの属性に依存する設計だったため、iPhone/iPadでは「ダウンロード」ではなく別タブでの表示(プレビュー)になり、ユーザーが手動で「共有→ファイルに保存」等の操作を追加で行う必要が生じる可能性がある(推測を含む)。対策候補として、Web Share API(`navigator.share`、ファイル共有に対応)経由でiOS標準の「ファイルに保存」シートを呼び出す方式が考えられるが、対応状況は未調査。
  - **クリップボードへのコピー(`navigator.clipboard.writeText`):** Safari 13.1以降でAsync Clipboard APIに対応しているが、iOS版では「ボタンクリックという正しいユーザー操作を伴っていてもNotAllowedErrorが出る」という報告が複数見つかった。5.11(チャットパレットコピー)・5.12(コピー&ペーストボタン)はこの機能が中核であるため、影響が大きい。対策候補として、非同期処理を挟まずクリック直後に同期的に呼び出す、または旧来の`document.execCommand('copy')`(非表示のtextarea等を使う方式)をフォールバックとして併用する、といった方法が一般的に使われている。
  - **この2点は今回の設計の中核機能(書き出し・ワンクリックコピー)に直結するため、11章の未確定事項として最優先で検証した。結果は下記「実機検証プロトタイプでの確認結果」の通り、いずれも解決済み。**
  - **CClogでの実績確認(2026-07-23、Mikoto確認済み):** 既に運用中のCClogでは、iPhone/iPadから「マークダウン」「テキストファイル」の書き出し(いずれも`<a download>`方式)が問題なくできているとの報告を得た。
  - **実機検証プロトタイプでの確認結果(2026-07-23、Mikoto実施):** 検証用プロトタイプ(11章参照)を、当初`file://`(ローカルファイル)で開いた際はクリップボードコピー・ファイル書き出し・共有機能のいずれも反応しなかった。これは`navigator.clipboard.writeText`と`navigator.share`がいずれも「セキュアコンテキスト」(実質HTTPS)を要求する仕様であることが原因と考えられた(MDN等で確認)。GitHub Pages(HTTPS)経由で同じファイルを開き直したところ、**クリップボードコピー・`<a download>`によるHTMLファイル書き出し・`navigator.share`のいずれも成功した。** これにより、少なくともHTTPSでホスティングする前提であれば、iOS SafariでもHTML書き出し・クリップボードコピーの両方が実現できる見込みが高いことが実機で確認された。**結論: 3章で懸念していたiOS Safariのファイル書き出し・クリップボードコピーのリスクは、HTTPSホスティング(GitHub Pages)を前提とする限り、大部分は解消されたと考えられる。** 残る課題は5.3の編集用ツールボックス(ボタン)の操作性(11章参照)。

## 4. 入力データの仕様・見出しパターンの調査結果

- 入力形式はPDF・TXTの両方に対応する方針(Mikoto確認済み、2026-07-23)。
- **解析対象の優先順位:** ~~TXT版・PDF版の両方が入手できる作品でも、PDF版を優先して解析する(2026-07-23、開発初期の判断)。理由: PDF版は作品によらず必ず存在するのに対し、TXT版は用意されている場合とされていない場合があるため、まずPDF解析を軸に据える。~~ **→ 2026-07-29、Mikoto要望により運用段階の方針としてTXT版を優先に変更。** 上記のPDF優先は「PDF抽出の精度を軸に開発を進める」という開発段階の優先順位であり、実際の運用では**TXT版の方が文章を安定して読み込める**(PDFはレイアウトによって行の並び順が乱れることがある、4章参照)ため、TXT版・PDF版の両方が入手できる場合はTXT版を優先して選ぶことを推奨する。ツール自体は選んだファイルをそのまま読み込む仕組み(5.1参照)のため、この優先順位はツール内で自動判定するものではなく、ユーザーがファイルを選ぶ際の目安として案内する。
- **抽出精度の目標:** PDFからのテキスト抽出は「なるべく綺麗に抽出する」ことを目標とする(Mikoto確認済み、2026-07-23)。レイアウト崩れ・改行位置のずれへの対応を高精度側に寄せて設計する。実装時にどこまでの精度が実現できるかは要検証。

- **サンプル調査結果1(2026-07-23、市販シナリオ「お迎えには傘が必要」BOOTH販売、TXT版・PDF版で確認済み):**
  - シーン見出しは「01.導入」のような「2桁数字+ピリオド+シーン名」という行パターンで、本文中に規則的に出現する。
  - 一方で「■シナリオチャート」という目次セクションにも同じ見出しが先に出現するため、単純な正規表現一致だけでは目次と本文を誤って両方拾ってしまう。

- **サンプル調査結果2・3(2026-07-23、Mikoto提供のPDF2作品「花雷（かみなり）」「LaLuLa」で確認):** pdftotextでテキスト抽出のうえ、見出しパターンをgrepで確認した。
  - 両作品とも、「01.導入」のような「2桁数字+ピリオド」形式の見出しは1件も出現しなかった(該当行0件を確認済み)。
  - 「花雷」: シーン見出しは「■」記号+シーン名(例:「■導入：入院一日目」「■入院一週間目」)。章内のさらに細かい場面は「【】」括弧の見出し(例:「【目が覚める】」)で構成されている。数字による連番はなく、目次・シナリオチャートに相当するセクションも見当たらない。
  - 「LaLuLa」: 章は「【英単語または場所名】」(例:「【Opening】」「【Day】」「【Night】」「【Hospital】」)、シーン単位は「▼」記号+短い日本語(例:「▼夢幻」「▼早朝」「▼異変」)という2階層構成。本文中に「【シナリオの読み方】」という凡例セクションがあり、記号の意味(【 】=各章、▼=シーン、▽=探索箇所、〈 〉=技能・ダイスロール箇所、■=PL開示情報)が明記されていた。
  - **結論:** 3作品を比較すると、見出しの表現方法(数字連番か、■か、【】+▼の2階層か)は作者ごとに大きく異なっており、単一の固定正規表現ではシーン分割を汎用的に自動化することは難しいと考えられる(推測です)。

- **方式の変遷(記録):** この調査結果を受け、当初は「ユーザーが見出し記号・書式(数字連番、■、【】など)を事前指定し、ツールが自動検出する」という半自動方式を採用した。実際にプロトタイプで検証したところ、指定した記号がシーン区切り以外(前付け・参考情報・エンディング・場所の階層分けなど)にも使われるケースが多く、非シーン項目が相当数混入することが分かった(検証の詳細は旧版の記録として本節末尾に残す)。この点をふまえてMikotoと再度相談した結果、記号パターンそのものが作者ごとに大きく異なりすぎるという4章の調査結果を踏まえ、**自動検出をやめて完全手動で見出しを設定する方式に変更した(2026-07-23、Mikoto確認済み。5.4参照)。**

  <details><summary>旧方式(記号指定による半自動検出)のプロトタイプ検証記録</summary>

  Pythonスクリプトで花雷・LaLuLaの抽出テキストに対して検証した。花雷を「■」記号で分割すると44件検出されたが、実際のプレイ場面は一部で、前付け(PL向け概要など)・参考情報(暗黒世界、呪文：火花など)・エンディング(ENDAなど)も同じ■記号で書かれているため混在した。LaLuLaを「【】」章+「▼」シーンで分割すると38件の「章」が検出されたが、実際のプレイ順の章(Opening/Day/Nightなど)以外に、前付け(概要・真相・NPC)やHospital内の場所階層(一階：警備本部など)も同じ【】記号のため同列検出された。また、見出し番号が本文の連番として大きく連続する「まとまり」を検出し目次との重複を除外するロジック、フラット検出した見出しに手動でレベルを指定して複層構造を組み立てるロジックについても、それぞれPythonプロトタイプで動作を確認していた。これらの検証で得た「複層構造を手動レベル指定で組み立てられる」という知見は、5.5(見出し階層構造)にそのまま引き継いでいる。

  </details>

## 5. 機能要件

### 5.1 ファイル読み込み

PDF・TXTの両形式を読み込めること。両方が入手できる場合は**TXT版を優先して選ぶ**ことを推奨する(2026-07-29改訂、理由は4章参照。PDFはレイアウトによって抽出結果が乱れることがあるため、安定して読み込めるTXT版がある場合はそちらを優先する運用とする)。

- **不具合修正(2026-07-24、Mikoto報告・修正済み):** Shift-JIS(CP932)で保存されたTXTファイル(「君よ寝てはならぬ.txt」で発覚)を読み込むと文字化けしていた。従来はUTF-8決め打ちで読み込んでいたが、まずUTF-8として厳密デコードを試み、失敗した場合はShift-JISとして読み直す方式に修正した。本ツールが書き出すHTML(常にUTF-8)には影響しない。

- **複数ファイルの読み込み(2026-07-27、Mikoto要望):** シナリオがシーンごとに複数のTXTファイルに分かれて執筆されている場合があるため、複数ファイルをまとめて読み込めるようにした。ファイル選択時に複数選択でき、**選んだ順に上から連結**して1本の本文として扱う。PDF・TXT・本ツールで書き出したHTMLを混在させて選ぶこともでき、HTMLが含まれる場合はその見出し・NPC等の編集データも引き継ぐ(複数ある場合、保存日時などのメタ情報は最初のHTMLのものを採用する)。

- **ファイルの読み込み直し・追加読み込み(2026-07-27、Mikoto要望):** ホーム画面に追加した状態(スタンドアロン表示)ではブラウザのリロード機能が使えず、一度ファイルを読み込むと別のファイルに切り替えられなくなるため、ハンバーガーメニューの**最下部に「ファイル」セクション**を追加した。編集モード・プレビューモードのどちらでも表示される。
  - **別のファイルを読み込み直す(入れ替え):** 現在の内容を破棄して読み込み直す。破棄前に確認ダイアログを出す。
  - **ファイルを追加で読み込む(末尾に追加):** 現在の本文の末尾に読み込んだ内容を連結する。シーンごとに分かれたTXTを後から継ぎ足す用途を想定。この操作は「元に戻す」(5.17)で取り消せる。
  - 現在読み込んでいるファイル名もこのセクションに表示する。なお「元に戻す」で追加読み込みを取り消した場合、本文とNPCは元に戻るが、この表示上のファイル名は連結されたままになる(表示だけの情報のため、実害はないものとして許容している)。

### 5.2 編集モード・プレビューモードの切り替え

ツールに「編集モード」と「プレビューモード」の2つの表示モードを持たせる(Mikoto要望、2026-07-23)。

- **編集モード:** PDF/TXTから抽出した本文を表示し、5.3の編集用ツールボックスを使って、見出しの設定(5.4)・文字装飾(5.6)・コピー&ペーストボタンの設置(5.12)などを行う。階層調整は5.5のアウトラインビューで行う。準備段階で主に使うモード。
- **プレビューモード:** 編集用ツールボックスを非表示にし、トグル見出しの開閉・ジャンプ目次・NPCカード・チャットパレットコピー・コピー&ペーストボタンなど、実際にセッションで使う完成形の見た目・挙動だけを表示する。セッション本番で主に使うモード。

**切り替えUI(Mikoto確認済み、2026-07-23):** 画面上部に「編集|プレビュー」のセグメントコントロール(タブ形式の切り替えUI)を常時表示する。

**ハンバーガーメニューの中身もモードに応じて切り替える(Mikoto確認済み、2026-07-23):** 編集モードでは「アウトライン(見出し管理、5.5)」「NPC管理(5.10)」、プレビューモードでは「目次(ジャンプ、5.9)」「NPCカード閲覧(5.10)」を表示する。

### 5.3 編集用ツールボックス

**方式(Mikoto確認済み、2026-07-23、当初の「Wordの上部リボン」イメージから変更):** ~~画面上部に常時ボタンを並べる固定リボン形式ではなく、本文中のテキストを選択した際に、その選択範囲の直後にふわっと現れる小さな「ミニツールバー」形式を採用する。iOSの標準コピー&ペーストメニューに近い操作感で、選択していない間は本文の表示領域を広く使える。~~ **→ 2026-07-25に再度方針転換。** 実際にiPhone/iPadの実機でテストしたところ、選択範囲に追従する形式だとOS標準のコピー&ペーストのポップアップメニューと表示位置が重なり、ボタンがタップできなくなる不具合が判明した(Mikoto報告)。そのため、当初検討していた「画面上部に常時ボタンを並べる固定リボン」形式に戻した。ヘッダー直下に常時ドッキングして表示し(スクロールしても画面上部に固定)、本文を選択していないときはボタンが無効化(グレーアウト)される。狭い画面でボタン全体が収まらない場合は折り返さず横スクロールする。ミニツールバー(リボン)には以下のボタンを並べる。

1. **元に戻す(Undo):** 直前の編集操作を取り消す(2026-07-25、Mikoto要望。詳細は5.17参照)。
2. **見出し設定:** 選択範囲を見出しとして設定する(5.4参照)。見出しレベル(大見出し/小見出しなど)はこの場では指定せず、あとで5.5のアウトラインビューから調整する(Mikoto確認済み、2026-07-23。「とりあえず見出し化し、後で一覧から階層を調整」)。
3. **コピー&ペーストボタンの設置・NPC化・内部リンク:** 選択範囲専用のワンクリックコピーボタンを設置する(5.12参照)ボタン、NPCカードを作成する(5.10参照)ボタン、内部リンクを設定する(5.13参照)ボタンをこの順に並べる。コピペボタン設置と同様、NPC化・内部リンクのボタンもアイコンのみの表示にする(2026-07-25、Mikoto要望)。
4. **文字装飾:** 下線・イタリック・ボールドなどの装飾を選択範囲に適用する(5.6参照)。**並び順(2026-07-25、Mikoto要望):** 上記3のNPC化・内部リンクボタンより後ろに配置する。
5. **検索・一括置換(🔍)/空行削除:** 旧「編集ツール」セクションから移設(下記参照)。選択範囲を必要としない操作のため、他のボタンと違って選択の有無に関わらず常時押せる。

**サイドバーの「編集ツール」をリボンへ移設(2026-07-27、Mikoto要望):** サイドバーに表示するコンテンツが増えて縦に長くなりすぎたため、ハンバーガーメニューにあった「編集ツール」セクション(空行の一括削除・検索・一括置換)をリボン側へ移した。これによりサイドバーは「書き出し/アウトライン/NPC管理/ファイル」の4セクションになった。
  - **空行削除:** リボンのボタンを押すと即実行する(挙動は従来と同じ)。
  - **検索・一括置換:** リボンの🔍で、ヘッダー直下に検索バー(`.find-bar`)を開閉する形にした。入力欄はリボンに直接置けないため、ブラウザの検索バーに近い形式を採用している。詳細は5.16参照。
  - **配置(Claude判断):** 🔍と空行削除は「元に戻す」の直後(見出しグループより前)に置いた。リボンは横スクロールするため末尾に置くと狭い画面で見えなくなるが、この位置なら375px幅の画面でもスクロールせずに届く。上記3・4で決めた「見出し→コピペ→NPC化→リンク→文字装飾」の並び順自体は変えていない。

**マークの解除(2026-07-24、Mikoto要望):** 見出し・文字装飾(下線・イタリック・ボールド)・コピー&ペーストボタンは、設定済みの範囲を選び直して同じボタンをもう一度押すと解除できる(トグル方式)。見出し解除は見出し設定だけを取り消し、配下の本文は残る(本文ごと削除する手段は現時点で用意していない)。

**段落全体を選択したときに解除できずマークが二重になる不具合(2026-07-27、点検で発見・修正済み):** トリプルクリックなどで段落全体を選択して同じボタンを押すと、解除されずに同じマークがもう1つ入れ子で作られてしまっていた(`<span class="h-mark">`の中にさらに`<span class="h-mark">`ができる状態)。見出しが二重になるとアウトライン・目次に同じ見出しが2つ並び、トグル構造も二重に入れ子になる。原因は、解除対象の判定が「選択範囲の境界ノードから**祖先**を辿る」方式だけだったこと。段落全体を選ぶと選択範囲の境界が`<p>`自身になり、マークは`<p>`の**子孫**にあるため祖先を辿っても見つからなかった。祖先に見つからない場合に限り「選択範囲がマーク要素をまるごと覆っているか」も判定するよう修正した(部分的に重なっているだけの選択では誤って解除しないよう、完全に覆っている場合だけを対象とする)。見出し・文字装飾・コピペボタンの3種すべてに適用。

**リボンの配色をライトモード対応(2026-07-27、Mikoto報告・修正済み):** リボン(`.mini-toolbar`)の背景・ボタンは元々ダークモードを前提にした固定色(背景`#263238`・ボタン`#37474f`・白文字)だったため、ライトモードでは周囲(明るい配色)から浮いて暗く見えてしまっていた。`--toolbar-bg`/`--toolbar-btn-bg`/`--toolbar-btn-text`/`--toolbar-sep`というCSS変数を新設し、ライトモードは明るい配色(背景`#dcdcdc`・ボタン`#f3f3f3`・文字`#222222`)、ダークモードは従来通りの配色を保つようにした。ボタン押下時のハイライト(`--accent`のオレンジ)は元々白文字だったため、ライトモード時も視認性を保てるよう明示的に白文字を指定している。

**複数段落にまたがるコピー&ペーストボタンの解除不具合(2026-07-25、Mikoto報告・修正済み):** 空行を含む複数段落にまたがる範囲に「コピペボタン設置」を適用した後、同じ範囲を選び直して解除しようとしても、空行部分だけコピー処理(`.cp-wrap`)が残ってしまう不具合があった。原因は、1つの`<span>`で複数の`<p>`をまたいで包んでいたため(`Range.extractContents()`が複数の`<p>`を含むフラグメントを返し、それを1つのspanに入れていた)、HTML構造として`<p>`の中に`<span>`、その中にさらに`<p>`が入るという不正なネストになり、範囲の再選択時に解除ロジックが正しく対象を見つけられなかったこと。段落ごとに個別の`<span class="cp-wrap">`で包み(コピーボタン自体は最後の段落にのみ設置)、同じ`data-cpid`を共有させることで「1つのコピー範囲」として扱うように修正した。解除時は同じcpidを持つ`.cp-wrap`をすべて取り除く。

**上記修正の副作用としての表示崩れ(2026-07-25、Mikoto報告・修正済み):** 段落ごとに個別の`.cp-wrap`で包む方式に変更した直後、見た目が1行ごとに区切られたブロックとして表示されてしまう副作用が生じた(各`.cp-wrap`がそれぞれ独自の枠線・背景・余白を持つため)。解除機能(cpid共有によるまとめての取り消し)はそのまま維持しつつ、表示だけ1つの引用ブロックのようにつながって見えるよう修正した。各`.cp-wrap`に先頭/中間/末尾を表す`cp-wrap--first`/`cp-wrap--mid`/`cp-wrap--last`のいずれかを付与し、CSSで継ぎ目の余白・角丸を調整するとともに、末尾以外の段落は`<p>`要素自体の下マージンを一時的に0にして段落間の隙間を消している(解除時は元に戻す)。

### 5.17 元に戻す(Undo)

**機能内容(2026-07-25、Mikoto要望):** 直前の編集操作を1つ取り消せる「元に戻す」ボタンをリボン(5.3)に設置する。

**実装方式(Claude判断):** `contenteditable`のネイティブUndo(Ctrl+Z等)は、見出し化・文字装飾・コピペボタン設置・NPC化・内部リンク設定・NPC追加/削除・見出しの並べ替え/階層変更・空行一括削除・検索と置換(5.16)といった、本ツール側でJavaScriptから直接DOM操作している変更を追跡できないため、独自にUndo履歴を持つ方式にした。上記の各操作の直前に、本文(`editorBody`)のHTMLとNPC一覧(`state.npcs`)をまとめてスナップショットとして履歴に積み、「元に戻す」ボタンで直前のスナップショットに復元する。履歴は最大20件まで(古いものから破棄)。新しいファイルを読み込んだ際は履歴をリセットする。NPC編集フォーム内の文字入力など、1文字ごとの変更までは追跡しない(フォーカス中のテキスト入力欄自体が持つネイティブUndoに委ねる)。

### 5.4 見出しの手動設定

**完全手動方式とする(Mikoto確認済み、2026-07-23、方針転換)。** 見出し表現が作品ごとに大きく異なり(4章参照)、記号による自動検出では非シーン項目の混入や、記号パターン自体の作品差という2つの問題が避けられなかったため、自動検出は行わず、ユーザーが本文中の任意のテキストを選択し、5.3の編集用ツールボックスから「見出し」として設定する方式に変更した。

これにより、以前課題になっていた「記号がシーン区切り以外にも使われて非シーン項目が混入する」という問題は、そもそも自動検出を行わなくなったため発生しなくなった(2026-07-23時点の整理)。前付け・参考情報・エンディングなど、見出しとして必要な部分だけをユーザーが選んで見出し化すればよい。

**選択範囲の粒度(Mikoto確認済み、2026-07-23):** 段落・改行単位に制限せず、任意の文字範囲を自由に選択できる方式とする。実機検証プロトタイプで、段落をまたいだ任意範囲の選択・装飾適用が実際に動作することを確認済み(3章参照)。この方針は見出し設定(5.4)・文字装飾(5.6)・コピー&ペーストボタン設置(5.12)すべてに共通する。

### 5.5 見出し階層構造(複層構造対応・並べ替え)

**アウトラインビュー(Mikoto確認済み、2026-07-23):** 見出しの並べ替え・階層調整は、本文中で直接行うのではなく、見出しだけを一覧表示する別画面(アウトラインビュー)で行う。編集モードのハンバーガーメニューから開く(5.2参照)。CClogの一覧編集(並べ替え・削除・追加のボタン方式)と同じ思想を踏襲する。

アウトラインビューでは、5.4で手動設定した見出しについて以下の調整ができること。

1. 見出し位置の削除・移動
2. 見出し名の編集
   - **コピペボタン付きの見出しで名前欄が壊れる不具合(2026-07-27、点検で発見・修正済み):** 見出しにした段落にコピー&ペーストボタンが設置されていると、見出し名の取得に要素のテキスト全体を使っていたため、ボタンのラベル(「📋コピー」)まで見出し名に混入していた(アウトライン・目次・書き出しHTML・TXT書き出しのすべてに影響)。さらにアウトラインの名前欄はその文字列を初期値にしていたため、**名前を編集しなくても入力欄からフォーカスを外すだけで、ボタンが「📋コピー」という文字列に化けてボタン自体が失われていた。** 見出し名の取得時にコピペボタンを除外する処理(`headingLabelText`)を全箇所に適用し、名前の書き換え時もボタンを壊さずラベル文字だけを差し替えるようにした(`setHeadingLabelText`)。
3. **階層レベル変更(インデント/アウトデント):** 各見出しについて、階層レベル(大見出し/その下の小見出し、といった親子関係)を手動で上げ下げできること(Mikoto要望、2026-07-23)。どれを大見出しにし、どれを小見出しにするかはツール側で自動判定せず、ユーザーの手動判断に委ねる。5.3のミニツールバーで見出し化した直後は、暫定的なレベル(例: すべて同一レベル)で追加され、ここで最終的な階層を決める。

**一覧表示の視認性(2026-07-24、Mikoto要望):** 「上へ/下へ/上位/下位」の並べ替え・階層変更ボタンは、デフォルトでは折りたたんでおき、「編集」ボタンを押した見出しだけ展開する。編集タブでアウトライン一覧を見たときにボタンが多く視認性が下がるのを防ぐため。「見出し解除」「本文ごと削除」ボタンは廃止し、見出しの解除は5.3のマーク解除(同じ範囲を選び直して見出しボタンを再度押す)で行う。

**上記の続報での不具合修正(2026-07-24、Mikoto報告):**
- 「編集」ボタンが枠からはみ出す不具合を修正。原因は見出し名入力欄(input)がflexboxの初期値`min-width:auto`により縮小せず、ボタンを外側へ押し出していたこと。inputに`min-width:0`を明示して解消した。あわせて「編集」ボタン自体も、開閉状態がひと目で分かるディスクロージャー用の小さなトグルボタン(▸/▾)に置き換えた。
- ハンバーガーメニューを閉じたときに、開いていた「編集」トグル(アウトライン・NPC管理どちらも)を自動的に畳んだ状態にリセットするようにした。次回メニューを開いたときに前回の展開状態が残らない。
- 「上へ/下へ/上位/下位」ボタンが見出し数の多いときに折り返して2行になる不具合を修正。ラベルを矢印記号のみ(▲▼◀▶)に短縮し、詳細な説明はtitle属性(長押し・ホバーで表示)に移した。4つのボタンをflex:1で均等割りにし、常に1行に収まるようにした(タップしやすさを保つため各ボタンの最小高さは38pxを確保)。
- 編集タブに、空の段落(空行)をまとめて削除するボタン「空行をまとめて削除」を追加(ハンバーガーメニュー内の新セクション「編集ツール」)。見出しや本文が入っている段落は対象外。
- 書き出し(HTML/Markdown/テキスト選択+書き出すボタン)を、画面右下の固定バーからハンバーガーメニュー内に移設した。デスクトップのプレビューモードでも書き出し操作にアクセスできるよう、その場合にハンバーガーボタンを隠す従来のCSSルールを削除した。

- **複層構造(ネスト)対応の検証記録:** LaLuLaの「Hospital」大見出しの中に「一階：警備本部」「二階：病棟」などの小見出しが入る、という2階層を超えるネスト構造を扱えるようにしたいという要望があった。Pythonプロトタイプ(見出しをフラット検出→手動レベル指定→木構造化)で検証し、以下の通り正しく親子関係を組み立てられることを確認した。3階層以上への対応も同じ仕組みで理論上可能と考えられる(推測です。実際に3階層以上のサンプルでは未検証)。当時は記号による自動検出結果に対して検証したが、完全手動方式(5.4)になった現在も、「見出しをフラットなリストとして持ち、手動でレベルを指定して木構造化する」という内部構造自体は同じ考え方で流用できる見込み(推測です)。

    ```
    - 【Hospital】
      - 【一階：警備本部】
      - 【一階：検査室】
      - 【二階：病棟】
      - 【図書室】
      - 【三階】
      - 【四階】
    ```

### 5.6 文字装飾(下線・イタリック・ボールド)

5.3のミニツールバーから、選択範囲に下線・イタリック(斜体)・ボールド(太字)の装飾を適用できる機能(Mikoto要望、2026-07-23)。HTML出力では対応するタグ(`<u>` `<em>` `<strong>`)を使う。

**Markdown変換ルール(Claude判断で暫定決定、2026-07-23。技術的な部分の判断はMikotoから一任済み):** ボールドは`**太字**`、イタリックは`*斜体*`という標準的なMarkdown記法に変換する。下線はMarkdownに標準記法がないため、インラインHTMLの`<u>下線</u>`をそのまま出力する(Notion側での見え方は未検証。11章参照)。複数装飾の重ねがけ(太字+イタリックなど)は、対応するタグ・記法を入れ子にして両方適用する。具体的なショートカットキー対応は実装時に検討する。

### 5.7 内容種別の自動判別について

地の文(情景描写)・判定情報・探索情報といった内容種別ごとの自動判別・自動タグ付けは、v1では行わない(Mikoto確認済み、以前の検討時点。自動判別は元シナリオのフォーマット差が大きく難易度が高いと判断されたため)。見出し配下の本文はまとめて格納する。内容種別の自動判定は将来検討事項とする(9章)。

### 5.8 トグル見出し化

5.5で確定した見出し階層構造をもとに、出力するHTML内の各見出しを開閉可能な「トグル見出し」として実装する(Mikoto要望、2026-07-23)。シーンやエンディングごとに折りたたんでおき、必要な箇所だけ開いて閲覧できるようにする。階層構造(5.5)をそのままトグルの入れ子に反映する。

### 5.9 ジャンプ見出し(目次)の自動生成

5.5で確定した見出し階層構造から、ページ内の該当箇所へジャンプできる目次を自動生成する(Mikoto要望、2026-07-23)。参考にした類似ツール(12章)でも「編集中の見出しから目次を自動生成」「自動ジャンプ見出し作成」という機能があり、方向性として妥当と考えられる。

**表示方式(Mikoto確認済み、2026-07-23):** モバイル(iPhone/iPad縦持ちなど画面が狭い場合)ではハンバーガーメニューから呼び出し、サイド(ドロワー)に表示する。iPadの横持ちやPC(デスクトップ)など画面に余裕がある場合は、常時表示のサイドパネルとする。プレビューモードのハンバーガーメニュー内の項目として位置づける(5.2参照)。

**ブレークポイント(Claude判断で暫定決定、2026-07-23):** 画面幅900px以上で常時表示サイドパネル、900px未満(iPhone全般、iPad縦持ち)でハンバーガー+ドロワーに切り替える。iPadは機種によって縦持ちの幅が768〜834px程度と幅があるため、900pxを暫定的な閾値とした。実機での見え方を見て調整する(11章参照)。

### 5.10 NPCカード機能

NPCのステータス・設定をカード形式にして一覧できる機能(Mikoto要望、2026-07-23)。

- **カードに含める項目(Mikoto確認済み、2026-07-23):** 能力値・技能値・プロフィール文。画像(アイコン・表情差分など)は不要(管理機能として実装しない、Mikoto再確認済み)。
- **データの取り込み方法(Mikoto確認済み、2026-07-23、方針転換):** 自動抽出は目指さない。当初は「自動抽出を試みつつ手動でも編集できる」方針で検討していたが、NPCが登場しないシナリオもあるため、全て手動入力とする方針に変更された。
- **表示場所(Mikoto確認済み、2026-07-23):** ハンバーガーメニューから呼び出せるサイドパネルに格納する。編集モードでは登録・編集用の「NPC管理」として、プレビューモードでは閲覧・チャットパレットコピー用の「NPCカード」として、同じ場所に表示内容を出し分ける(5.2参照)。
- **自動抽出方針の再転換(2026-07-24、Mikoto確認済み):** 上記の「全て手動入力」の判断は、5.4の見出し自動検出撤回(作品ごとに記号・書式が違いすぎる)と同じ理由付けで下したものだったが、Mikotoから複数シナリオの実例(イハス/忌まわしき混血児/芳野葵/五十嵐杏弥)が示され、NPCステータス欄は見出しほど自由記法ではなく、STR/CON等の決まった略語や判定コマンドの書式に一定の規則性があることが確認できた。そのため、本文選択からの半自動抽出を採用する。
  - **操作:** ミニツールバー(5.3)に「NPC化」ボタンを追加。選択範囲の生テキストからNPCカードを新規作成し、NPC管理の編集フォームをその場で開いて確認・修正できるようにする(選択範囲の本文自体は変更・削除しない)。
  - **抽出ロジック:** 能力値(STR/CON/POW/DEX/APP/SIZ/INT/EDU)とその他ステータス(HP/MP/SAN/耐久力/正気度/装甲/BLD/MOV/DB)は、区切り文字(「：」「:」半角スペース・区切りなし)の違いを吸収しつつラベル+数値のペアとして抽出する。技能・チャットパレット行は、判定コマンド(CCB<=/CC<=を含むトークンや素のダイス表記)で始まる行をそのままコマンド+ラベルとして採用する。本文中に「【チャットパレット】」の見出し行がある場合はそれ以降の行だけを対象にし、それより前の地の文にある「回避：26％」のような重複表記は無視する(実例で地の文とチャットパレット欄の両方に同じ技能が書かれているケースがあったため)。
  - **判定コマンドのバージョン(6版CCB/7版CC):** シナリオ本文からは自動判定せず、NPCごとに手動設定する(Mikoto確認済み: バージョン判定は自動化しない方針)。「回避：26％」のようにコマンドが明記されていない行から自動生成する際にこの設定を使う。
  - **安全策:** 抽出結果が誤っていても情報が失われないよう、選択範囲の全文は常にプロフィール欄にもそのまま保持する。名前欄は選択範囲の1行目をそのまま初期値にする(Markdownの太字記号`**`や「〇」等の箇条書き記号のみ除去し、それ以上の区切り推測は行わない)。
  - **表記パターンの追加対応(2026-07-24、Mikoto確認済み):** 「＜触手＞90 ダメージ 1D6」「＜回避＞32」のような`＜技能名＞数値`形式にも対応した(「【チャットパレット】」に加えて「技能」という見出し行も、以降の行だけを対象にする区切りとして認識する)。ステータス欄の値が「装甲：なし」のように数値でない場合もカード上にはそのまま表示する(ただし5.11のJSON出力のstatusには数値必須のため出力されない)。
  - **判定コマンドを後から変更した場合(2026-07-24、Mikoto指摘・修正済み):** NPC編集フォームの「判定コマンド」プルダウンを変更すると、既存の技能行のうちCCB<=/CC<=形式の単純なコマンドは新しい版に自動で置き換わる(「sCCB<=95」のような独自表記やダイス表記のみの行は対象外のまま残す)。
  - **「名詞＋パーセンテージ」形式を基準形に統一(2026-07-25、Mikoto確認済み):** 「回避：26％」のようにコロン区切りのみ対応していたが、「回避　32％」のように全角/半角スペース区切り、および「回避32％」のように区切りなしの表記にも対応した。ユーザー側がこの「名詞＋パーセンテージ」の形になるよう本文を手動調整して選択する運用とし、これ以外の表記ゆれをすべて自動対応することは目指さない(Mikoto確認済み)。
  - **基準形どおりでも認識されない不具合(2026-07-27、Mikoto報告・修正済み):** 上記の基準形で書いていても技能として自動認識されない場合があるとの報告を受けて調査したところ、当時の実装が「行全体に完全一致する正規表現」(`/^(.+?)[：:\s]*([0-9０-９]+)％(?:\s+(\S.*))?$/`)だったことに起因する、以下4点の取りこぼしが判明した。行全体への一致をやめ、「数値＋％」のトークンを行内から走査して拾う方式(`extractPercentSkillsFromLine`)に変更して修正した。
    1. **半角の「%」がまったく認識されなかった**(全角の「％」しか許容していなかった)。PDF・TXT由来のシナリオでは半角「%」も普通に使われるため、これが主因と考えられる。
    2. **数値と％の間に空白があると認識されなかった**(例:「回避 32 ％」)。
    3. **％の直後に空白なしで文字が続くと行全体が不一致になった**(例:「回避 32％。」のように句読点や閉じ括弧が付いている場合)。
    4. **1行に複数の技能が並んでいると先頭の1件しか拾えなかった**(例:「目星 25％ 聞き耳 30％」)。PDFからの抽出で複数行が1行にまとまってしまう場合に該当する。
  - **技能名の決め方(2026-07-27、上記修正にともなう仕様):** 技能名は「直前の技能(または行頭)からその数値までの間のテキスト」から、区切り文字(コロン・スペース)や前の技能から続く句読点を除いたものとする。技能名自体に数字は含まれない前提とし、名前の中に数字が残る場合(例:「STR 10 幸運 50%」のように同じ行に別のステータスが並んでいる場合)は、最後の数字より後ろだけを技能名として扱う。ダメージ表記(`1d3+1D4`など)を探す範囲は、その技能の直後から次の技能の手前まで。

### 5.11 チャットパレットのワンクリックコピー

NPCカードから、ココフォリアのチャットパレットとして直接貼り付けられるプレーンテキストを、ワンクリックでクリップボードにコピーできる機能(Mikoto要望、2026-07-23)。

- **書式(Mikoto提供のサンプルにもとづく、2026-07-23):** `[判定コマンド]<=[数値・演算・変数] 【技能名など】` という行を1技能につき1行並べる形式。判定コマンドは6版で`CCB`、7版で`CC`(Mikoto確認済み)。SAN値のような可変値は`{SAN}`のような波括弧の変数参照、能力値×5判定は`CCB<=7*5`のような演算込みの表記、ダメージ判定は`1d3+0`のような単純なダイスロール表記になる。サンプル一式は E:\右腕AI\TRPGツール開発スペシャリスト\シナリオ整形ツール\検証用サンプル\チャットパレット記法サンプル.md に保存済み。
- **JSON形式対応をv1に前倒し(Mikoto確認済み、2026-07-23、方針転換):** 当初は「まずプレーンテキストで動くものを作り、JSON対応は将来検討」としていたが、可能であればv1からココフォリアのコマ登録用JSON形式にも対応することになった。
- **JSON形式の仕様(2026-07-23、CCFOLIA公式ドキュメント`docs.ccfolia.com/developer-api/clipboard-api`で確認済み):** ココフォリアは`{ "kind": "character", "data": { ... } }`という形のJSON文字列をクリップボードに置き、盤面に直接ペーストすることでキャラクター駒を生成する仕様になっている(ファイルインポートではなく、クリップボード経由のペースト操作)。`data`の`Character`型には`name`(名前)・`status`(HP/SANなどのステータス配列)・`params`(能力値配列)・`commands`(文字列)などのフィールドがあり、`commands`フィールドに、上記のプレーンテキスト形式のチャットパレット文字列をそのまま格納できる見込みである。これにより、名前・ステータス・チャットパレットを含んだ駒を1回のペーストで盤面に生成できる。
- **重要な訂正(2026-07-23):** 「JSON形式にすればクリップボードコピーの問題が解決するのでは」というMikotoの想定について確認したところ、ココフォリアのJSON取り込みも上記の通りクリップボード経由のペーストが前提であり、プレーンテキストのチャットパレットと同じくクリップボードへの書き込み(`navigator.clipboard.writeText`等)に依存する。そのため、JSON形式への対応は3章のiOS Safariクリップボード問題そのものを回避する手段にはならない(訂正済み)。JSON対応は、駒の生成を1ステップで済ませられるという別の利点からv1採用を決定した。クリップボードの信頼性問題自体は、引き続き11章の最小プロトタイプ検証で確認する。
- **実装・実機確認(2026-07-24、Mikoto確認済み):** NPCカード機能(5.10)とあわせて実装した。ボタン名称は「📋 チャットパレットをコピー」(プレーンテキスト側)・「📋 ココフォリア駒出力」(JSON側)に確定。
- **能力値が駒に反映されない不具合の修正(2026-07-24、Mikoto報告・修正済み):** 実機テストで、名前・ステータス・チャットパレットは反映されるが能力値だけ反映されないことが判明した。公式ドキュメントをあらためて直接確認したところ、`params[].value`は文字列型、`status[].value`/`max`は数値型と型が異なる仕様であり、実装時は両方とも数値に変換していたのが原因だった。`params[].value`を文字列のまま出力するよう修正済み。あわせて、`status`側は数値化できない値(「装甲：なし」「DB：2d6」など)をJSON出力からは除外する(NPCカード上の表示・チャットパレットのテキストには引き続き残す)仕様にした。

### 5.12 コピー&ペーストボタン(旧称: 便利ボタン)

**名称変更(Mikoto確認済み、2026-07-23):** 「便利ボタン」から「コピー&ペーストボタン」に改称。

**機能内容(Mikoto確認済み、2026-07-23):** 5.4〜5.5の見出し単位の設定とは別に、ユーザーが本文中の任意のテキスト範囲(段落の一部、複数文にまたがる範囲など)を選択し、5.3のミニツールバーから設置すると、その範囲専用のワンクリックコピーボタンがHTML内のその位置に設置される機能。**選択・設置のUIは5.3のミニツールバーに統合されることが決定した(2026-07-23)。**

**表示方式(Mikoto確認済み、2026-07-23):** 設置すると、選択範囲の直後に小さなコピーボタンが常時表示される(検証用プロトタイプで実際に確認した方式と同じ)。プレビューモードでも編集モードでも、このボタンは常に見えている状態にする。

例(Mikoto提供): 「探索者たちは～～目の前のそれは、人知を超えた神話的な存在である。あなたたちは恐怖を覚え、正気度喪失1/1d3」というSAN値喪失の描写文をユーザーが選択すると、その範囲を1クリックでコピーできるボタンがその場所に表示される。

見出し単位(シーンやNPCカード)の粒度に縛られず、GMが「ここは実際に読み上げる・貼り付ける」と判断した任意の範囲にボタンを設置できる点が特徴。

**不具合(2026-08-01、Mikoto報告・修正済み):** プレビューモードでコピーすると、同じ文章が2回繰り返してコピーされていた(「AはBへ移動したAはBへ移動した」のような形)。複数段落にまたがる範囲に対応するため、同じcpidを持つ`.cp-target`を`document`全体から集めて連結していたことが原因。プレビューは本文を複製して作られるため、編集エリアとプレビューの両方に同じcpidの要素が存在し、同じ範囲を二重に数えていた。探索範囲を、クリックされたボタンが属するエリア(`#preview-content`または`#editor-body`)の内側に限定して修正した(`copyScopeOf`)。同じ理由でdocument全体を見ていたコピペボタンの解除処理(`removeCopyButtonMark`)も同様に限定した。

**あわせて修正した不具合(2026-08-01、上記の調査中に発覚):** 書き出したHTMLでは、複数段落にまたがるコピー範囲のうち**最初の1段落分しかコピーされなかった**。`data-copy-text`に埋め込む文字列を`querySelector`(単数)で取得していたため。すべての`.cp-target`を連結するよう修正した。

**コピー範囲の中でのEnter(2026-08-01、Mikoto報告・修正済み):** 通常の本文はEnter1回で段落が変わるのに、コピー範囲の中では1回だと単なる改行になり、2回押さないと段落が変わらなかった。`.cp-wrap`は`<p>`の中に入れられるよう`<span>`で作っている(`display:block`で見た目だけブロックにしている)ため、ブラウザがEnterで`<p>`を分割せず`<br>`を入れていたことが原因。さらに`<br>`は`textContent`に現れないため、コピーしたときの改行が欠落し、貼り付け後の空行の数も不揃いになっていた。

- Enterの判定は`beforeinput`の`insertParagraph`で行う(iOSのソフトウェアキーボードではキーの内容が当てにならないため)。コピー範囲の外では従来どおりブラウザに任せる。
- 段落の分割を自前で行い、**複数段落のコピー範囲と同じ構造**(段落ごとに1つの`.cp-wrap`を持ち、同じcpidを共有する)に揃える(`splitCopyWrapAtCaret`)。分割後は`syncCopyWrapChain`で`--first/--mid/--last`・下マージン・ボタン位置(最後の段落だけ)を再計算する。
- 既存の文書やShift+Enterで残る`<br>`も、コピー時は改行として扱う(`copyTargetText`)。

**行の区切りは改行2つ(2026-08-01、Mikoto要望):** 複数段落にまたがるコピー範囲をコピーしたとき、行の間に**空行が入る**ようにした(改行2つでつなぐ。`COPY_BUTTON_LINE_SEPARATOR`)。ココフォリアなどに貼り付けたときの見た目を、ツール上の表示に近づけるため。編集タブ・プレビュー・書き出したHTMLの3経路すべてで同じ区切りになる。**本文を手でコピーする場合(5.19)は逆に改行1つ**で、目的が違うため区切りを分けている。NPCカードのチャットパレット・ココフォリア駒出力は独自の形式を持つため、この変更の対象外。

### 5.18 本文の構造の正規化

**背景(2026-08-01、Mikoto報告への対応):** 段落を`\n`でつないだHTMLを`innerHTML`に流し込むと、`<p>`と`<p>`の間に改行だけのテキストノードが残る。見た目には影響しないが、これは**段落のどこにも属さない編集可能な位置**であり、タップ位置の解決を不安定にする要因になる。ブラウザの保存情報から復元したときにカーソルが意図しない場所に置かれる、という報告を受けて対処した。

**仕様:** ファイル読み込み・追加読み込み・自動保存からの復元の直後に`normalizeEditorStructure()`を通し、本文の直下を**ブロック要素だけ**の状態に整える。

- 空白だけのテキストノード(段落の外にある意味のない編集位置)は削除する。
- 段落に包まれていない裸のテキスト・インライン要素は`<p>`で包み直す。
- 分割されたテキストノードを`normalize()`で結合する。
- 空行(`<p><br></p>`)は従来どおり保持する。

**原因の特定(2026-08-11にMikotoがiPhoneで書き出した実データを解析。2026-08-01の推測を裏付けた):** Mikotoから提供された実機書き出しHTML(`CoC「明日雨になあれ.」_整形済_整形_整形_整形.html`、4回往復したもの)を解析したところ、**iOS(WebKit)が編集中に差し込む`<span style="...">`が21個混入**していた。内訳は`font-size: 0.95rem; -webkit-text-size-adjust: 100%`が13個、`-webkit-text-size-adjust: 100%`のみが6個、`font-size: 0.95rem`のみが2個。**うち1つは中身が空**(`<span style="font-size: 0.95rem;"></span>`)だった。

- **空の`<span>`が直接の原因。** 見た目には何も無いのにカーソルを置ける位置を作るため、タップ位置の解決を狂わせる。中身のあるものも、テキストを余計な入れ子に分断してカーソルの当たり判定を不安定にする。
- ファイル名の「_整形_整形_整形」が示すとおり、往復のたびに痕跡が蓄積する。サニタイズは`<span>`と`style`属性を許可しているため、そのまま保存・復元されていた。
- 自動保存でだけ目立ったのは、**書き出しが区切りのついた状態で行われるのに対し、自動保存は編集の途中を数秒ごとに拾う**ため、痕跡を含んだ状態を掴みやすいからと考えられる(この点は推測)。
- あわせて、**中身が空の`.cp-target`が4件**見つかった。高さが潰れて見えないのにカーソルを置ける位置になっており、同じ問題を起こす。

**対処:** ツール自身はクラスの無い`<span>`を作らない(コピー範囲・見出し・検索マークはすべてクラスを持つ)ため、**クラスの無い`<span>`は編集の痕跡と判断して外す**(`stripEditingArtifacts`)。読み込み・復元時の正規化に加え、**自動保存と書き出しの時点でも取り除く**ので、痕跡が蓄積しなくなる。空の`.cp-target`は`<br>`で1行分の高さを確保し、通常の空行と同じ扱いにする。

**検証(実データで実施):** 上記のファイルを読み込み、痕跡21個→0個、空の`<span>`4個→0個、段落外のテキストノード357個→0個になることを確認。本文(427段落・見出し15・コピー範囲47)は完全に保たれる。読み込み直後と自動保存からの復元後の両方で、タップ位置とカーソルの行き先を15,652点ずつ実測し、ずれは0件。書き出し直しても痕跡は再混入しない。

### 5.19 本文内でのコピー・切り取り

**不具合(2026-08-01、Mikoto報告):** 編集タブで本文をコピーして貼り付けると、コピー元にはなかった**空行が行ごとに挟まる**。メモアプリなど他のアプリからの貼り付けでは正常だった。

**原因:** ブラウザ既定のプレーンテキスト化では、段落(`<p>`)の区切りが**改行2つ**として書き出される。本ツールは1行を1つの`<p>`で表しているため、行の数だけ空行が生まれていた。実測で、既定では`"第一章 館\n\n探索者は館に入る。\n\n\n\n読み上げ一行目。"`のようになることを確認した(空行はさらに倍増する)。

**修正:** 本文の`copy`・`cut`でプレーンテキストを自分で組み立て、**段落の区切りを改行1つ**にした(`selectionToPlainText`)。コピー元と同じ見た目で貼り付く。

- コピペボタンのラベル(「📋コピー」)は本文ではないため持ち出さない。
- `<br>`は改行として扱い、ブラウザが高さ確保のために置く行末の`<br>`は落とす。空行はそのまま1つの空行として保たれる。
- 段落をまたがない選択(1つの段落の内側だけ)は、そのまま文字列にする。
- 切り取りは既定動作を止めた分、Undo履歴に積んだうえで削除を自前で行う(段落構造の扱いはブラウザに任せる)。

### 5.13 内部リンク

文書内の任意の場所(例: シーン本文からNPCカードへの参照、他シーンへの参照)にリンクを貼れる機能(Mikoto要望、2026-07-23)。**設定方法は手動設定とする(Mikoto確認済み、2026-07-23)。** 自動検出は行わない。内部リンクを使う場面はそう多くないとの判断による。

- **実装(2026-07-24、Mikoto確認済み):** 5.3のミニツールバーに「リンク」ボタンを追加。選択範囲に対してクリックすると、見出し一覧・NPC一覧から1件選ぶピッカーが開き、選ぶとその場でリンクが設定される。リンク先は見出し・NPCカードの両方に対応する。
- **クリック時の挙動:** 編集モードでは何も起きない(誤操作・カーソル移動との競合を避けるため、Mikoto確認済み)。プレビューモードでのみ、見出しへのリンクはトグルを開いてスクロール、NPCカードへのリンクは目次・NPCパネル(5.2・5.9参照。画面幅に応じてハンバーガードロワー/常時表示インラインパネルを自動判別)を開いて該当カードまでスクロール・一時ハイライトする。書き出したHTML(単体ファイル)でも同じ挙動が動作する。
- **Markdown出力での扱い(2026-07-24、Claude判断で暫定決定):** 見出しへのリンクは`[テキスト](#スラッグ)`形式で出力する。スラッグは小文字化・空白をハイフン化する一般的な規則にもとづく暫定実装で、**日本語見出しでの実際の動作(Notion側のアンカーと一致するか)は未検証**(11章7番の検証は英単語見出し1件のみ)。NPCカードへのリンクは、NPCカード自体がMarkdown出力に含まれない(5.10)ためリンク化はできず、ラベルの文言だけを出力する。

### 5.14 出力形式

以下3形式での書き出しに対応する(Mikoto確認済み、2026-07-23)。

1. **HTML:** トグル見出し・ジャンプ目次・NPCカード・チャットパレットコピー・コピー&ペーストボタン・内部リンクを備えた、セッション本番で使うインタラクティブなファイル。セッション時はこのHTMLをiPhone・iPadで開き、そこから地の文やチャットパレットをコピペする(Mikoto確認済み、2026-07-23)。
2. **Markdown:** セッション終了後にNotionへ読み込んで記録として保存するためのエクスポート形式。**書式方針(2026-07-23、Mikoto確認済み。2026-07-23中に3回、2026-07-24に1回、計4回改訂あり):**
   - **全階層を`<details><summary>見出し</summary>`の直後に空行を挟んで本文を続け、`</details>`で閉じるトグルリスト形式で統一する(2026-07-24、4回目の改訂で確定)。**
     - 改訂の経緯: 当初は大見出し→`###`・小見出し→`####`・3階層目以降のみトグルリスト化という方針だったが、実際にNotionへ読み込んだところ`###`/`####`は単なる見出しになりトグル化されないことが判明した。そこで一度は全階層をトグルリスト形式に統一したが、Mikotoが求めていたのは「トグルリスト」ではなくNotion側の「トグル見出し」ブロック(見出しとして機能しつつ折りたたみもできるもの)だったことが判明した。Notionには見出し4ブロックが実装されており、トグル見出し3は`###>`というショートカットでNotion内に直接タイプすれば作成できることをMikotoが確認したため、これをMarkdown書き出しに反映したが、**貼り付け・インポート経由では`###>`は見出しにならずただのトグルリストになってしまうことが実機検証で判明した**(Notionのショートカットの一部は、キー入力時にのみ働くものと貼り付け時にも働くものがあり、`###>`は前者だったと考えられる)。トグル見出しは貼り付け経由では作成できないという結論にいたり、一時は「最上位の見出しは通常の見出し4(トグルなし)、それ以外はトグルリスト」という方針に落ち着いていたが、**最上位の見出しも折りたたみたいという要望を受け(2026-07-24)、全階層をトグルリスト形式に統一した(4回目の改訂)**。見た目上は「見出し」ではなく「トグルリスト」の項目になる点は変わらない。
   - NPCカード・コピー&ペーストボタンは記録用途に不要のため、Markdown出力には含めない。
   - 内部リンク(5.13)は標準Markdownのアンカーリンク形式`[テキスト](#見出しスラッグ)`を用いる(2026-07-23、実機検証で確定。11章7番参照)。
3. **TXT:** プレーンテキストでの書き出し。**用途(Mikoto確認済み、2026-07-23):** 内容の簡単な確認と、Notion以外の場所へコピペするための汎用テキストとして使う。**書式(Claude判断で暫定決定、2026-07-23):** 記号・Markdown記法は使わず、見出しはそのままの文字列を階層レベルに応じたインデント(スペース)を付けて改行区切りで並べ、その下に本文を続ける、というシンプルな構成にする。特定のツール(Notion等)への取り込みを前提としない、どこにでも貼り付けやすい素のテキストとする想定。

**HTMLの上書き保存(2026-08-01、Mikoto要望):** 作業途中で何度も書き出すたびに新しいファイルが増え、フォルダが煩雑になるという問題があった。Wordの保存のように、一度保存したファイルを同じ場所に上書き更新していく形にしたいという要望を受けて実装した。**HTML形式のみが対象**で、MarkdownとTXTは記録用の最終成果物という位置づけのため、従来通り毎回新規ダウンロードのままにしている。

- **仕組み:** ブラウザのFile System Access API(`showSaveFilePicker`)で保存先ファイルへの参照(ハンドル)を取得し、保持する。初回の「書き出す」で保存ダイアログが開き、保存先を決めたあとはボタンのラベルが「上書き保存」に変わり、2回目以降はダイアログなしで同じファイルを上書きする。保存先のファイル名はボタンの下に「保存先: 〇〇.html」と表示する。区切りのいいところで別ファイルに分けたい場合のために「別名で保存」ボタンを併設した。
- **対応環境の制約(重要):** このAPIはChrome/Edge系のデスクトップブラウザでのみ利用でき、**Safari(iOS/macOS)とFirefoxは非対応**。iPhone/iPadではOS側に「ローカルのファイルを上書きする」概念自体がない。そのため非対応環境では機能検出で自動的に従来の`<a download>`方式(毎回新規ダウンロード)にフォールバックし、上書き保存関連のUIも表示しない。Mikotoの運用は「PCで整形・保存 → 生成したHTMLをiPhone/iPadで開いてセッション」という流れのため、フォルダが煩雑になるPC側でだけ効く形になる。
- **保存先の記憶(IndexedDB):** ハンドルはJSONに直列化できずlocalStorageに入らないため、IndexedDBに保存する。これによりブラウザを閉じて開き直したあとも保存先を引き継げる。ただし**リロード後の初回保存時に一度だけ、ブラウザによる書き込み許可の確認ダイアログが出る**(ブラウザ側のセキュリティ仕様で省略できない)。
- **誤って別のシナリオを上書きしないための判定:** ファイルを読み込み直したタイミングで保存先を一度手放し、**読み込んだファイル名が記憶している保存先のファイル名と一致する場合にのみ引き継ぐ**。つまり「前回保存したHTMLを開き直した」ときだけ上書き先が復活し、別のシナリオを読み込んだ状態では新規保存扱いになる。
- **失敗時の扱い:** 書き込み許可が得られなかった場合はその旨をトースト表示し、保存先は保持したまま(再試行できる)。保存先ファイルが移動・削除されているなどで例外が出た場合は、参照が無効になっているため保存先を手放し、新規保存からやり直せる状態に戻す。

**iPhone・iPadでの代替手段「共有して保存」(2026-08-01、Mikoto要望):** iOSにはWebページからローカルファイルを直接上書きする仕組みが存在せず、上記の上書き保存は使えない。代わりに、HTMLをiOSの共有シート(Web Share API)へ渡す「共有して保存」ボタンを用意した。共有シートの「ファイルに保存」で、前回と同じ場所に同じ名前で保存しようとするとiOSが「置き換え」を提案するため、実質的な上書きになる。毎回フォルダを辿る手間はあるため、ワンタップの上書きにはならない。

- 表示条件は`navigator.canShare({ files: [...] })`による機能検出で、HTML形式を選んでいるときだけ表示する。非対応環境では現れない。
- **実機確認の結果(2026-08-01、Mikoto報告):** iPhoneで「共有して保存」ボタンは表示された。iOS SafariはHTMLファイルの共有に対応している。
- **不具合(2026-08-01、Mikoto報告・修正済み):** 「ファイルに保存」でテキストファイルとHTMLファイルの2つが保存されてしまっていた。`navigator.share()`に`files`と一緒に`title`を渡していたため、iOSがタイトル文字列を別の共有項目として扱い、テキストファイルとして書き出していたことが原因。共有内容を`files`1つだけに変更した。
- **結論(2026-08-01、Mikoto実機検証で確定):** 2ファイル問題を修正した後も、同じ場所に同じ名前で保存したときにiOSは「置き換え」を提案せず、別ファイルとして増える。**iOSの共有シート経由の保存に置き換えの選択肢は存在しない**ことが確定した。したがってこの機能は上書き保存の代替にはならず、**「保存先フォルダを選べる」だけの手段**である(標準のダウンロード先以外に置きたいときに使う)。ボタンはその用途で残す(Mikoto判断、2026-08-01)。
- **設計上の教訓:** 「同名保存ならiOSが置き換えを提案するはず」という前提でこの機能を提案したが、実機では成立しなかった。iPhoneでのファイル増加問題は**5.17の自動保存で解決する**のが正しい方針であり、共有シートは補助的な位置づけにとどめる。

### 5.17 自動保存と復元

**背景・目的(2026-08-01、Mikoto要望):** iPhoneでは5.14の上書き保存が使えないため、「一時保存のたびにファイルが増える」問題を別の角度から解決する。作業途中の内容をブラウザ内に自動保存し、次に開いたときに続きから再開できるようにすることで、**一時保存のためにファイルを書き出す行為そのものを不要にする**。HTMLの書き出しは「セッションで使う完成版を出す」ときだけになるので、ファイルは増えようがなくなる。PCでも同じく有効で、5.14の上書き保存と併用すれば作業中の事故に二重に備えられる。

**仕様:**

- 保存先はIndexedDB(5.14の保存先ハンドルと同じDBを共用。オブジェクトストアは分ける)。保存する内容は本文HTML・NPCカード・元のファイル名・保存時刻。
- 保存の契機は、本文への直接入力(`input`イベント)と、見出し化・NPC作成・アウトライン操作などの変更処理(`scheduleAutoRender`経由)。**最後の変更から3秒後**にまとめて保存する。保存操作のUIは持たない。
- 元のファイル名をキーとして、**直近5シナリオ分**を保持する。それを超えた分は古いものから自動的に削除する。
- 読み込み画面に「前回の続き」セクションを表示し、保存済みの一覧(ファイル名と「〇分前」などの相対時刻)から**復元**または**削除**を選べる。保存データがないときはセクションごと非表示。
- **検索マーク(`.search-hit`)は保存に含めない**(5.16の方針と同じ)。実行中の検索を邪魔しないよう、本文の複製側からマークを外してから保存する。
- 復元時は、ファイル読み込みと同じサニタイズ(`sanitizeLoadedBodyHtml`・`sanitizeLoadedNpcs`)を必ず通す。保存領域が後から書き換えられている可能性を想定した措置(8.1)。一覧に出すファイル名も`textContent`で挿入する。
- 復元後は、5.14の保存先も同じ判定ルールで引き継ぐ(ファイル名が一致する場合のみ)。

**負荷への配慮(2026-08-01):** 自動保存のたびに本文全体を複製すると、長いシナリオではiPhoneで数秒おきの負荷になる。検索マークが無いとき(大半のケース)は複製せず、本文のHTMLをそのまま使う。

**注意点(制約として明記しておくもの):** iOS Safariは、一定期間サイトを使わないとブラウザ内の保存データを自動削除することがある(ITPの仕様。7日程度と言われているが、**ホーム画面に追加したスタンドアロン状態での正確な挙動は未検証**)。したがって自動保存はあくまで作業中の保険であり、**完成したらHTMLに書き出しておく運用は引き続き必要**。またIndexedDBが使えない環境(プライベートブラウズなど)では自動保存は黙って無効になり、編集自体は通常どおり続けられる。

### 5.15 生成済みHTMLの読み込み・プレビュー

3章の通り、変換機能と同じツールで、一度生成したHTMLファイルを読み込んでプレビュー・再編集できるようにする(Mikoto確認済み、2026-07-23)。**復元範囲の方針(Mikoto確認済み、2026-07-23):** 出力した時と同じ構造(見出し階層、トグルの開閉状態を除く構造そのもの、NPCカード、コピー&ペーストボタンの設置位置、文字装飾、内部リンクなど)が完全に再現されるようにする。

**隠しデータ構造(Claude判断で暫定決定、2026-07-23):** CClogが採用している「見た目部分はそのまま表示できるHTML+画面には出ない隠しJSONデータ」という二重構造をそのまま踏襲する。書き出すHTMLファイル内に`<script type="application/json" id="scenario-tool-editor-state">`のようなタグを設け、見出しツリー・NPCカード・コピー&ペーストボタンの設置位置・文字装飾・内部リンク・schemaVersion・savedAtなど(6章のデータモデル相当)をJSONとして埋め込む。読み込み時にこのタグの有無を見て、「本ツールで書き出した編集可能なファイル」か「まだ見出し等を設定していない生のPDF/TXT由来のファイル」かを判定する(CClogの5.4「読み込み時の自動判定」と同じ考え方)。具体的なJSON構造・バージョン管理の細部は実装時に詰める。

### 5.16 検索・一括置換

**機能内容(2026-07-24、Mikoto要望):** 本文中の特定の文字列(例:「KPC」)を検索し、一括で別の文字列(例:「{KPC}」)に置換できる機能。

**設置場所(2026-07-27、Mikoto要望により変更):** 当初はハンバーガーメニューの「編集ツール」セクションに置いていたが、サイドバーが縦に長くなりすぎたため、リボンの🔍で開閉する検索バー(ヘッダー直下に固定表示)に移した(5.3参照)。構成は以下の通り。
  - 1段目: 検索する文字列の入力欄 / 「検索」ボタン / 「↑」(前の一致箇所へ) / 「↓」(次の一致箇所へ) / 「×」(閉じる)
  - 2段目: 置換後の文字列の入力欄 / 「一括置換」ボタン
  - 3段目: 「「KPC」3件中 1件目」のような状態表示
  - 検索する文字列の入力欄は検索と一括置換で共用する(下記「検索したワードをそのまま置換」参照)。入力欄でEnterキーを押しても検索できる。
  - プレビューモードに切り替えると自動的に閉じる(編集モード専用の機能のため)。

**検索と一括置換を別ボタンに分離(2026-07-27、Mikoto要望):** 当初は「検索と置換」として1つのボタンにまとめていたが、置換を伴わず単に本文中の一致箇所を確認したいだけのケースのために、検索専用の操作を分離した。**この2つのボタンを1つに統合しないこと**がMikotoからの明示的な要望(2026-07-27)。検索(「検索」「↑」「↓」)は本文を書き換えないため、Undo履歴には積まない。

**一致箇所をすべてマークし、順番に巡回できるようにした(2026-07-27、Mikoto要望):** 当初は最初の1件を選択状態にするだけだったが、一致箇所が**すべて**色つきでマークされるようにした。あわせて「↓」(次の一致箇所へ)ボタンを設け、ワンクリックで次のマークへスクロールできるようにした(最後まで行くと先頭に戻る)。対になる「↑」(前の一致箇所へ)も用意している(Claude判断)。現在地のマークだけは他と区別できるよう、より濃い色+アクセントカラーの枠線で強調する。
  - **マークの扱い(Claude判断):** このマークは本文データではなく一時的な表示用のもの(`<span class="search-hit">`)。そのため、プレビュー描画・書き出し(HTML/Markdown/TXT)・「元に戻す」の履歴・自動保存(5.17)・一括置換の前には必ず取り除き、保存データや出力ファイルに混入しないようにしている。
  - **マークを外すタイミングの見直し(2026-08-01、Mikoto報告による不具合修正):** 当初は「本文に触り始めた時点(mousedown/touchstart/keydown)」で外していた。マークの解除はテキストノードの分割状態を変えるため、選択が確定した後に外すと控えておいた選択範囲(savedRange)が無効になる、という理由による。**しかしこれは、タップの最中に本文のDOMを作り替えることを意味していた。** iOSはtouchstartで当てた位置をもとに指を離した時点でカーソルを置くため、その間にテキストノードが消えると、タップした場所と違う位置にカーソルが飛んだり、意図しない範囲が選択されたりする。iPhoneの編集モードで頻発していたカーソル・選択の不安定さの原因がこれだった(実測で、touchstart1回につき本文へ14件のDOM変更が発生していることを確認)。
    - **修正方針:** タップ中は一切DOMを触らないようにし、マークを外すのは**(1)実際に文字が入力された後**(`input`イベント)と**(2)リボンのボタンで見出し化・装飾を適用する直前**の2箇所だけにした。読む・カーソルを合わせるだけの操作ではマークは消えない。
    - savedRangeが無効になる問題は、範囲をノード基準ではなく**本文全体を通した文字位置**に変換してからマークを外し、外した後に貼り直す方式で解決した(`rangeToOffsets`・`offsetsToRange`・`clearSearchHighlightsKeepingRange`)。
    - 日本語入力の変換中(`isComposing`)はDOMを触らず、変換確定(`compositionend`)まで待つ。変換中に本文を作り替えると入力が壊れるため。
    - **挙動の変化(許容した差分):** 本文をタップしただけではマークが消えなくなった。検索した箇所を見て回る間もマークが残るため、むしろ使いやすい方向の変化と判断した。
    - **追加修正(2026-08-01、Mikoto実機報告):** 上記の修正後もiPhoneではタップだけでマークが消えていた。iOSは本文をタップしただけでも`input`・`compositionend`を発火させることがあるためと考えられる(実機でしか起きないため原因イベントの特定はできていない)。**発火したイベントの種類に頼らず、本文のテキストが実際に変わったときだけマークを外す**方式に変更して確実にした(マークを付けた時点のテキストを`searchBaseText`に控えておき、比較する)。マークは`<span>`で包むだけでテキストの中身を変えないため、この比較は成立する。

**検索したワードをそのまま置換に使えるようにした(2026-07-27、Mikoto要望):** 検索した後に「やはり置換したい」と思った際、一括置換側の検索欄にもう一度同じワードを入力し直す必要があり手間だった。検索バーへの移設(上記)にあわせて、検索する文字列の入力欄そのものを検索と一括置換で共用する形にしたため、検索したワードで続けて置換できる。ボタン自体は上記のとおり分離したまま。

**実装方式・既知の制約(Claude判断):** どちらも本文(`contenteditable`)内のテキストノードを1つずつ走査し、単純な文字列一致で検索する方式(検索・マークは`highlightAllMatches`、置換は`findAndReplaceInEditor`)。見出しマーク・文字装飾(太字/イタリック/下線)・コピー&ペーストボタン・NPC化した範囲・内部リンクなど、既存のHTML構造(タグ)はそのまま保持されるため、置換によってこれらの設定が崩れることはない。一方で、検索文字列が装飾タグなどで途中から分割されている場合(例:「KP」まで太字、続く「C」が太字でない、といった状態)は、テキストノード単位でしか見ていないため検出できない(既知の制約、検索・置換どちらも共通)。通常の固有名詞・略称の検索・置換であれば問題なく動作する想定。

## 6. データモデル

複数シナリオでのフォーマット差分検証がまだ3サンプルのみのため、確定的なデータモデルの提示は避ける(わかりません)。方向性として妥当と考えられる(推測です)フィールド構成は以下の通り。具体的な構造は実装時に確定する。

- 見出し項目: 見出しテキスト・階層レベル・子要素・本文
- 文字装飾: 装飾対象の範囲・装飾種別(下線/イタリック/ボールド)
- コピー&ペーストボタン: 対象テキスト範囲・設置位置
- NPCカード: 名前・ステータス項目・技能値一覧・プロフィール文
- チャットパレット: プレーンテキスト行のリスト、およびココフォリアJSON形式(`kind`/`data.name`/`data.status`/`data.params`/`data.commands`等、5.11参照)
- 内部リンク: 参照元・参照先のID
- モード状態: 編集モード/プレビューモードのいずれで開かれた状態を保存するか(5.2参照、要検討)

## 7. UI/UX要件

- PC・モバイル両方の画面サイズで操作できるレイアウトとする。
- 分割結果・NPCカードの確認・手動修正が直感的に行えることを優先する。
- CClogと同様、取り消しにくい操作(見出し削除など)には配慮する。
- トグル見出し・ジャンプ目次・NPCカードなど画面要素が多くなるため、情報量が多くても迷わない画面構成にする(具体的なレイアウトは実装時に検討)。
- **編集モードとプレビューモードで画面の見え方を明確に切り替える(5.2参照):** 編集モードでは上部に編集用ツールボックス(5.3)を表示し、プレビューモードでは非表示にする。
- **本文の1行の幅(2026-07-24、Mikoto報告・修正済み):** 画面幅の広いPCで本文が画面いっぱいに広がり、1行が長くなりすぎて読みづらかった。編集エリア・プレビューエリア・書き出しHTMLの本文とも、読みやすい幅(730px程度)に制限し中央寄せにした。あわせてフォントサイズもわずかに下げた。
- **空行の不具合修正(2026-07-24、Mikoto報告・修正済み):** PDF/TXT読み込み時、空行が`&nbsp;`(実体は空白文字)で埋められており、選択・コピー・NPC自動抽出時などに余分な空白として残ってしまっていた。`<br>`に変更し、空行の見た目の高さは保ちつつ、実際のテキスト内容は空にした。
- **編集エリアの説明文の横位置ずれ修正(2026-07-24、Mikoto報告・修正済み):** PC画面(横幅が広い場合)で、編集エリア上部の説明文(「本文をドラッグして選択すると...」)が左詰めのままになり、その下のグレー枠(`.editor-body`、`max-width:720px`で中央寄せ)と横位置がずれていた。説明文にも同じ`max-width:720px; margin:0 auto;`を適用し、位置を揃えた。
- **ダークモード/ライトモード対応(2026-07-24、Mikoto確認・対応済み):** OSの配色設定(`prefers-color-scheme`)に自動追従する。編集アプリ本体(`style.css`)・書き出したセッション用HTML(`app.js`内`STANDALONE_CSS`)の両方で、背景・文字・枠線・入力欄などの色をCSS変数化し、ダークモード用の配色を`@media (prefers-color-scheme: dark)`で上書きする形にした。トースト通知やミニツールバーなど、元々濃色固定で両モードとも視認性に問題がない要素はそのままにしている。ユーザーがOS側で手動切り替えを行う想定で、アプリ内に独自のテーマ切り替えスイッチは設けていない。
  - **ヘッダーは固有色で固定(2026-07-24、Mikoto要望):** アプリ上部のヘッダーバーは、ダーク/ライトどちらのモードでも同じブランドカラー(青)で表示されるよう、テーマに連動しない固定のCSS変数(`--header-bg`/`--header-text`)を用意した。ヘッダー上のモード切替ピル(編集/プレビュー)も、ヘッダー自身の色を基準にした固定配色にしている。
  - **より鮮やかな青に再調整(2026-07-24、Mikoto要望・2回目の色調整):** 当初のヘッダー色(`#2c5b78`)や`--accent`系(リンク・ボタン・見出しマークの帯線など)の青が地味だったため、Material Designの青系パレットを参考に、より鮮やかな青に統一した。`--header-bg`は`#1976d2`(固定、両モード共通)、`--accent`はライト`#2196f3`/ダーク`#64b5f6`、`--accent-dark`はライト`#1565c0`/ダーク`#90caf9`に変更。書き出したセッション用HTML(`STANDALONE_CSS`)側の`--accent`も同じ値に揃えた。
  - **主要ボタン(`.btn--primary`)をヘッダーと同色に統一(2026-07-25、Mikoto要望):** 「ファイルを選ぶ」「＋ NPCを追加」など`.btn--primary`を使うボタンの背景色を、`--accent`(テーマにより変化)から`--header-bg`(固定色)に変更し、ヘッダーと完全に同じ色になるようにした。
  - **青系ブランドカラーをオレンジに変更(2026-07-27、Mikoto要望):** ダークモードで青が見づらいとの指摘を受け、ヘッダー・主要ボタン・アクセントカラー(コピー範囲の帯線・内部リンクの文字色・アウトライン操作時のハイライトなど)を全面的にオレンジ系へ変更した。当初は独自に調整した深みのあるオレンジ(`#d84315`)を採用したが、「もう少し黄色に近いものに」との追加要望、続けて「iOSのsystem colorのorangeが想定」との具体的な指定を受け、最終的にAppleのHuman Interface Guidelinesで定義されているiOS標準の`systemOrange`(ライト`#FF9500`/ダーク`#FF9F0A`)をそのまま採用した。`--header-bg`(固定・両モード共通)は`#FF9500`。`--accent`はライト`#FF9500`/ダーク`#FF9F0A`、`--accent-dark`(見出し帯線・サイドバー見出し文字などのテキスト用途、白文字ボタンではないため独自に濃淡調整)はライト`#a35f00`/ダーク`#ffc770`。コピー範囲ブロックの背景色`--cp-bg`も帯線と合わせてオレンジ系の淡色(ライト`#ffe9cc`/ダーク`#3d2b0a`)に変更した。書き出したセッション用HTML(`STANDALONE_CSS`)側も同じ値に揃えた。
  - **メニュー見出し文字をさらに濃く調整(2026-07-27、Mikoto報告・修正済み):** ハンバーガーメニュー内の見出し文字(「編集ツール」「アウトライン」「NPC管理」など、`.sidebar-section__title`)が見づらいとの指摘を受け、`--accent-dark`のライトモード値を`#a35f00`から`#7a4700`(より濃いオレンジ)に変更した。この変数は見出しマークの帯線(`border-left`)にも使われているが、こちらは文字ではなく単なる装飾線のため、濃くなっても視認性上の問題はない。ダークモード側の値(`#ffc770`)は指摘対象ではなかったため変更していない。
  - **見出しマークの配色をグレー基調に変更(2026-07-24、Mikoto報告・修正済み):** 元々は黄色(`#ffe9a8`)だったが、ダークモードで黒背景に対して視認性が悪かったため、グレー基調に変更した。ライトモードでは白より少し暗いグレー(`#e6e6e0`)、ダークモードでは黒背景で目立つ明るめのグレー(`#7d7d78`)にしている。
  - **編集エリアの背景・枠線も同様にグレー基調へ変更(2026-07-24、Mikoto要望):** テキスト編集エリア(`.editor-body`)の背景・破線の枠(元は黄色系のクリーム色`#fffdf5`+枠線`#c9b98a`)も、見出しマークと同じ考え方でグレー基調に統一した。ライトモードは背景`#f0f0ec`+枠線`#c6c6c0`、ダークモードは背景`#2a2a2c`+枠線`#55554f`。見出しマークの方が編集エリア本体より明るい(ライトモードでは少し濃いめ、ダークモードでは明るめの)グレーになるようにして、マークが本文の中で目立つ関係性は維持している。
  - **見出しマークが目立たなくなった不具合の修正(2026-07-24、Mikoto報告・修正済み):** 上記のグレー化の直後、編集エリアの背景色(`--editable-bg`)と見出しマークの背景色(`--heading-bg`)がどちらも近いトーンのグレーになってしまい、特にライトモードでほぼ判別できないほど差がなくなっていた(`#f0f0ec`と`#e6e6e0`は差が小さすぎた)。ライトモードの`--heading-bg`を`#d8d8d0`、ダークモードを`#8c8c86`に変更してコントラストを広げ、さらに背景色の濃淡だけに頼らず判別できるよう、見出しマークの左側にアクセントカラー(`--accent-dark`)の帯線(`border-left`)を追加した。
- **ホーム画面追加時のアイコン(2026-07-27、Mikoto要望):** iPhone/iPad(および対応するAndroid端末)でページを「ホーム画面に追加」した際に、指定の画像がアイコンとして使われるようにした。指定画像をプロジェクト直下に`icon.png`としてコピーし、`index.html`に`<link rel="apple-touch-icon">`(iOSのホーム画面追加用)・`<link rel="icon">`(通常のfavicon)・`<link rel="manifest">`(Android等のホーム画面追加用マニフェスト`manifest.webmanifest`)を追加した。`theme-color`もヘッダーと同じオレンジに設定している。
  - **採用画像の変更(2026-07-27、Mikoto指定):** 当初は`C.png`(オレンジ地に白抜き「C」)を使っていたが、`S.png`(400×400、オレンジ地に白抜き「S」)へ差し替えた。`icon.png`の中身を入れ替え、ファイル名は変えていない。ブラウザ・OSはアイコンを強くキャッシュするため、参照側に`icon.png?v=2`とバージョンクエリを付けている。**すでにホーム画面に追加済みの場合、アイコンは追加時点のものが端末に保存されるため、新しいアイコンにするには一度削除して追加し直す必要がある。**
- **空行削除ボタンの配置(2026-07-24、Mikoto要望):** ハンバーガーメニュー内で「空行をまとめて削除」ボタン(編集ツール)が、書き出し操作のすぐ下に来るよう、アウトライン・NPC管理より上に配置した。
- **フォントの変更(2026-07-24、Mikoto要望):** 「Notionと同じフォントにしてほしい」との要望があったが、Notionが実際にどのフォント(font-family)を指定しているかをClaudeは正確には把握していない(ハルシネーション防止のため、この点は「わかりません」と明記する)。そのため、Notionそのものの再現ではなく、和文・欧文が混在する画面で読みやすいとされる代替フォントを採用した(推測です・実機での比較検証は未実施)。従来の`-apple-system, BlinkMacSystemFont, "Hiragino Sans", "Yu Gothic", sans-serif`に対し、Windows標準UIフォントの`"Segoe UI"`と、視認性重視で設計された和文フォント`Meiryo`、および`"Yu Gothic UI"``"Hiragino Kaku Gothic ProN"`を候補に追加した。編集アプリ本体(`style.css`)・書き出したセッション用HTML(`app.js`内`STANDALONE_CSS`)の両方に反映済み。実際にNotionの見た目に近づけたい場合は、具体的なフォント名(Webフォントの利用可否含む)を追って確認する必要がある。
  - **ヒラギノ系を優先(2026-07-24、Mikoto要望):** フォントスタックの並び順を変更し、`"Hiragino Sans"``"Hiragino Kaku Gothic ProN"`を先頭に配置した。ヒラギノ系はmacOS/iOS標準搭載のフォントで、Windowsには標準で入っていないため、Windows環境では従来通り`"Segoe UI"``"Yu Gothic UI"`等にフォールバックする(推測です。Windows機での実機表示は本セッションでは確認できていない)。本ツールの完成版HTMLは主にiPhone/iPadのSafariでセッション時に閲覧する想定(3章参照)のため、Mac/iOS環境ではヒラギノ系が優先的に使われるようにした。

## 8. 非機能要件

- ホスティングはGitHub Pagesとし、ツール本体(静的ファイル一式)のみを配置する(CClogと同様、3章参照)。シナリオデータ・変換結果はコミットしない。
- サーバー・データベース・ログイン機能を持たず、完全にクライアントサイドで完結する構成とする。
- 外部への通信(API呼び出し等)を必要としない設計とする(v1時点。9章の将来検討事項は除く)。
- モバイル(iPhone/iPad Safari)での動作を、プロトタイプの早い段階で実機検証する(3章のリスク参照)。

### 8.1 セキュリティ(2026-07-27、Mikoto依頼の点検にもとづき対応)

**前提とする脅威:** 本ツールは第三者が配布したシナリオ(PDF・TXT・本ツール形式のHTML)を読み込むことが主用途であり、読み込むファイルは**信頼できない外部データ**として扱う必要がある。またセッション用に書き出したHTMLを卓のメンバーへ配布するため、汚染が伝播する経路も想定する。点検で以下の3件について実際に攻撃が成立することを確認し、修正済み。

- **読み込んだHTMLによる任意コード実行(XSS、修正済み):** 隠しJSONの`bodyHtml`を検証せず`innerHTML`に代入していたため、細工したHTMLを読み込ませると`<img src=x onerror=...>`のようなイベントハンドラ属性が実行できた(実証済み)。対策として、本ツールが実際に生成しうるタグ・属性だけを許可するホワイトリスト方式の洗浄処理(`sanitizeLoadedBodyHtml`)を追加した。許可外の属性(`on*`・`src`・`formaction`等)は削除、`<script>`/`<iframe>`等は要素ごと削除、未知のタグは中身のテキストを残してタグだけ剥がす(シナリオ本文が失われないようにするため)。`<a>`のリンク先は同一ページ内アンカーに限定し、`javascript:`を排除する。
- **汚染の書き出しファイルへの伝播(修正済み):** 上記で汚染された本文は、書き出したセッション用HTMLにもそのまま引き継がれ、配布先で実行されうる状態だった。読み込み時点で洗浄する上記の対策により、汚染された本文がツール内に入らないようにした。
- **`data-hid`経由のスクリプト注入(修正済み):** 書き出しHTMLの目次リンクが`onclick="jumpToHeading('...')"`という形で`data-hid`を生のまま埋め込んでいたため、細工した`data-hid`で引用符を閉じて任意のJSを注入できた(実証済み)。`onclick`属性を廃止して`data-hid`を属性値としてエスケープし、クリック処理は書き出し先のJS側でイベント委譲する方式に変更した。あわせて、`data-hid`・`data-cpid`・`data-target`・NPCのidは読み込み時に英数字・ハイフン・アンダースコアのみへ正規化する(セレクタや属性値への埋め込みで悪用されないようにするため)。`escapeHtml`にシングルクォートのエスケープも追加した。

**あわせて実施した多層防御:**

- **pdf.jsの既知脆弱性(CVE-2024-4367)への対策:** 同梱している`vendor/pdfjs`はバージョン3.11.174で、細工したPDFのFontMatrix経由で任意のJavaScriptが実行される既知の脆弱性(4.2.67未満が影響)の範囲に入る。ライブラリの更新は影響範囲が大きいため、公式に案内されている回避策である`getDocument({ isEvalSupported: false })`を適用した。本ツールはPDFのテキスト抽出しか行わず描画しないため、無効化による機能上の影響はない。**根本対応(pdf.jsの更新)は将来検討事項として残る。**
- **貼り付けのプレーンテキスト化:** `contenteditable`の既定では書式付き(HTML)で貼り付くため、悪意あるWebページからコピーした場合にイベントハンドラ属性が本文へ混入しうる。`paste`をプレーンテキスト挿入に固定した(シナリオ整形の用途では書式ごと貼り付ける必要がないため、使い勝手への影響はない)。
- **書き出しHTMLへのCSP付与:** 書き出すファイル自体に`<meta http-equiv="Content-Security-Policy">`を付け、`default-src 'none'`で外部通信・外部読み込みを一切禁じ、スクリプトとスタイルはファイルごとに生成したnonce付きのものだけを許可する。これにより、万一本文にイベントハンドラ属性が紛れ込んでも、開いた先のブラウザ側で実行が止まる(CSPあり/なしの比較で、注入したインラインハンドラがCSPありでは実行されず、正規の目次ジャンプ・トグル・コピーボタンは正常動作することを確認済み)。

## 9. 将来検討事項(v1スコープ外)

- **Notion API連携による自動投稿:** v1のスコープには含めないが、Markdown出力の手動貼り付けから、将来的にAPI経由での自動反映へ発展させる可能性を残す。
- **内容種別の自動判別・自動タグ付け:** 地の文・判定情報・探索情報を種別ごとに自動でタグ付けする機能(5.7参照)。

## 10. スコープ外(v1)

- 地の文・判定情報・探索情報の内容種別自動判別・自動タグ付け(9章)
- Notion API連携による自動投稿(9章)
- **NPCの画像(アイコン・表情差分)管理:** 参考にした類似ツール(12章)には画像管理機能があったが、Mikoto確認済み(2026-07-23)で不要と判断され、恒久的にスコープ外とする。
- ~~NPCデータのシナリオ本文からの自動抽出~~ **→ 2026-07-24に方針転換、v1スコープに含める。** 5.10参照。実例にもとづきNPCステータス欄には一定の書式規則性があることが確認できたため、本文選択からの半自動抽出(ミニツールバー「NPC化」)を実装した。
- **見出しの記号指定による自動検出:** 4章・5.4の通り、記号パターンが作者ごとに異なりすぎるため撤回し、完全手動方式に変更した。恒久的にスコープ外として扱う。
- 内部リンクの自動検出(5.13の通り、手動設定のみとする)
- 複数シナリオフォーマットへの汎用対応の保証(4章の通り、現時点で3サンプルのみでの調査のため)

## 11. 未確定事項・要確認事項(実装前にご確認ください)

1. ~~iOS Safariの技術的制約への対応~~ **→ 解決済み(2026-07-23)。** 検証用プロトタイプ(E:\右腕AI\TRPGツール開発スペシャリスト\シナリオ整形ツール\プロトタイプ\verify-prototype.html)を、GitHub Pages(HTTPS)経由でMikotoの実機(iPhone/iPad)でテストし、範囲選択・見出し化/文字装飾/コピペボタン設置・クリップボードコピー・`<a download>`によるファイル書き出し・`navigator.share`による共有のすべてが正しく動作することを確認した(詳細な経緯は3章参照)。**結論: iOS Safariでの動作は、HTTPSホスティング(GitHub Pages)を前提とする限り、今回設計している機能群で技術的な障害にはならないと判断できる。** 今後の実装でも、ボタンの選択範囲取得には`mousedown`+`preventDefault`と`selectionchange`監視を併用し、`touchstart`での`preventDefault`は使わない、という今回得た実装知見を踏襲する。
   - **不具合修正(2026-07-24、Mikoto報告・修正済み・その1):** ミニツールバーが選択範囲に追従せず、画面中央下寄りに無関係な位置で表示されることがあった。原因は、選択範囲の開始点(anchorNode)がeditorBody内にあるかしか確認しておらず、ドラッグが本文の外(書き出しバーなど画面に固定表示されている要素)まではみ出した場合、選択範囲の外接矩形が画面全体に近い大きさになってしまうことだった。開始点・終了点の両方がeditorBody内にあることを確認するよう修正し、本文の外にはみ出した選択のときはミニツールバーを表示しないようにした。
   - **不具合修正(2026-07-24、Mikoto報告・修正済み・その2):** 本文内で完結する複数行・複数段落の選択でも、依然としてミニツールバーが選択位置と無関係な中央付近に表示されることがあった。原因は、`Range.getBoundingClientRect()`が、段落全体が選択されている行の間に生じる「画面幅いっぱいに近いギャップ矩形」まで含んだ外接矩形を返すためだった。`Range.getClientRects()`で得られる個々の行の矩形を使うように修正した。
   - **表示位置の調整(2026-07-24、Mikoto要望):** 上記の修正時点では選択範囲の最下行の上に表示していたが、最上行の上に表示してほしいとの要望を受け、`getClientRects()`の先頭要素(選択範囲の最上行の実際のテキスト矩形)を使うように変更した。
   - **実機(iPhone/iPad)でのOS標準ポップアップとの重なり(2026-07-25、Mikoto報告・修正済み):** GitHub Pagesで公開したページを実機テストしたところ、選択範囲追従型のミニツールバーが、OS標準のコピー&ペーストのポップアップメニューと表示位置が重なり、ボタンがタップできなくなる不具合が判明した。位置計算をやめ、画面上部に常時ドッキングするリボン形式に変更した(5.3参照)。この変更に伴い、上記3件の位置計算に関する不具合修正はいずれも不要になった(リボンは常に固定位置に表示されるため)。
2. ~~編集用ツールボックス(5.3)の詳細UI~~ **→ 大枠決定(2026-07-23)、2026-07-25に再度リボン形式へ変更。** 当初Wordリボン形式から、選択時に出るミニツールバー形式に変更したが、実機テストでOS標準ポップアップとの重なりが判明したため、画面上部常時表示のリボン形式に戻した(5.3参照)。中身(元に戻す・見出し設定・コピー&ペーストボタン設置・文字装飾・NPC化・内部リンク)は確定。
3. ~~本文選択→見出し化のインタラクション詳細(5.4)~~ **→ 決定(2026-07-23)。** 段落・改行単位に制限せず、任意の文字範囲を自由に選択できる方式とする(5.4参照)。
4. ~~編集モード⇄プレビューモードの切り替えUI(5.2)~~ **→ 決定(2026-07-23)。** 画面上部のセグメントコントロール(編集|プレビューのタブ)で切り替える(5.2参照)。ハンバーガーメニューの中身もモードに応じて出し分ける。
5. ~~ジャンプ目次の画面幅による切り替えの実装方法~~ **→ 暫定決定(2026-07-23、Claude判断)。** 画面幅900pxを閾値とする(5.9参照)。実機での見え方次第で調整の余地あり。
6. ~~文字装飾のMarkdown/HTML変換ルール(5.6)~~ **→ 暫定決定(2026-07-23、Claude判断)。** 太字`**text**`・斜体`*text*`は標準Markdown記法、下線は`<u>text</u>`のインラインHTML(5.6参照)。Notion側での下線の見え方は未検証。
7. ~~Markdownのトグルリスト記法(5.14)~~ **→ 解決済み(2026-07-23、実機検証)。** 検証用サンプル(E:\右腕AI\TRPGツール開発スペシャリスト\シナリオ整形ツール\検証用サンプル\トグル記法・内部リンク検証サンプル.md)を用いて、Notionへの「コピー&ペースト」「インポート(Markdown & CSV)」の両方で候補パターンを比較検証した(両方式で結果は同一)。結論は以下の通り。
   - **3階層目以降の見出し:** `<details><summary>見出し</summary>` の直後に空行を挟んで本文を続け、`</details>`で閉じるHTML形式を採用する。Notion上で正しくトグルリストブロック化し、本文も折りたたみ内に格納されることを確認済み(本文を`<p>`タグで囲む方式でも同様に成功したが、空行を挟む形がより簡潔なため採用)。`>`+スペースによるトグル記法は引用ブロックになってしまうため不採用、見出しレベルを増やす方式はNotionが見出しを3段階(Heading 1〜3相当)までしか持たず4階層目以降が同じ見た目に潰れてしまうため不採用。
   - **内部リンク(5.13):** 標準Markdownのアンカーリンク形式 `[テキスト](#見出しスラッグ)` を採用する。ページ名のみを指定するリンク形式は外部リンク扱いになり意図通り機能しなかったため不採用。
8. ~~HTML読み込み時の隠しデータ構造の具体設計(5.15)~~ **→ 暫定決定(2026-07-23、Claude判断)。** CClogと同じ「見た目HTML+隠しJSON」方式(5.15参照)。JSON構造の細部は実装時に確定。
9. ~~TXT出力の具体的な書式~~ **→ 暫定決定(2026-07-23、Claude判断)。** 記号・Markdown記法を使わず、階層に応じたインデント+改行区切りのシンプルな構成(5.14参照)。
10. PDFテキスト抽出の実際の精度(4章で「なるべく綺麗に抽出したい」という目標を確認済み。Mikoto確認済み、2026-07-23、「実装時に調整する」との回答のとおり、実装時に検証する)。

## 12. 参考にした類似ツール(2026-07-23、Mikoto提供)

- **Loredit(正常ラヂヲ、BOOTH販売):** TRPGシナリオの執筆支援エディタ。Windows専用デスクトップアプリ(Microsoftストア版はサブスクリプション制)。実装機能: 2〜3段組、自動ジャンプ見出し作成、ショートカット、スニペット、文字数カウンター、SAN値喪失カウンター、文字装飾・ルビ、ワンクリックコピー便利ボタン、CoC・エモクロアのキャラクターシート読み込み→ココフォリア用コマ生成、内部リンク、書き出し(.txt/.pdf/.md/.html)、プレビュー機能。プラグインでPDF/MD取り込みにも対応。**モバイル動作は明言されていない(Windows11で動作確認、としか記載なし)。**
- **TRPGシナリオエディタ(しるべどん屋、BOOTH販売、無料体験版あり):** 単一HTMLファイル(trpgシナリオエディタ.html)で動作するシナリオ執筆支援ツール。実装機能: 見出し・引用・文字装飾・ルビ、見出しからのサイドパネル目次自動生成、NPCプロフィール・ステータス・表情差分アイコンの一元管理とグループ分類、NPCデータからワンクリックでココフォリア用コマJSONをコピー、GMメモ・秘匿情報・ハンドアウト等の装飾ボックス、吹き出し/かぎかっこ形式の会話文、プロジェクトファイル(.scnproj)保存・読み込み、ZIPエクスポート(有料版限定)。**「動作確認済み: Google Chrome, Microsoft Edge」「スマートフォン、タブレットでの動作は保証しておりません」と明記。**
- **共通の注意点:** 両ツールとも「GMがツール内で直接シナリオを執筆する」エディタ型であり、今回目指す「既存のPDF/TXTシナリオを土台に、手動で見出し・装飾・ボタンを設定していく」という方向性と近い部分もある(特にしるべどん屋の見出し・装飾・目次自動生成・NPCカード・ワンクリックコピーの実装イメージは参考になる)。

---

以上を要件定義とする。iOS Safariの技術的制約(11章1.)およびMarkdownのトグルリスト記法・内部リンク記法(11章7.)は実機検証により解決済み。次のワークフローステップは、11章に残る実装時の細部(編集用ツールボックスのアイコンデザイン・はみ出し対策、ジャンプ目次のブレークポイントの実機調整、HTML読み込み時の隠しJSON構造の具体スキーマ、TXT書式の実装、PDF抽出精度の検証など)を詰めつつ、本実装(実際のシナリオ整形ツール本体の開発)に進む。
